const NEWLINE = '\n';
const EMPTY = '';
const SPACE = ' ';
const ENCODINGFORMAT = 'utf8';
const wcOptions = ['l','w','c'];
const filecounts = {
  l: 'lineCount',
  w: 'wordCount',
  c: 'characterCount'
};
const TOTAL = 'total';

module.exports = {
  NEWLINE,
  EMPTY,
  SPACE,
  ENCODINGFORMAT,
  wcOptions,
  filecounts,
  TOTAL
};